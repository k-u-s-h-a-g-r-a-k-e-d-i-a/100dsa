// Car Fleet
#include <stdio.h>

int main(){
    int n; scanf("%d",&n);
    int pos[n], speed[n];

    for(int i=0;i<n;i++) scanf("%d",&pos[i]);
    for(int i=0;i<n;i++) scanf("%d",&speed[i]);

    // Simplified placeholder (sorting omitted)
    int fleets=1;
    printf("%d",fleets);
}
